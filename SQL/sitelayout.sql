delete from site_layout;

	-- site_id int not null,
	-- seq int not null,
	-- machine_id int not null,
	-- span int not null

-- Edinburgh machine workshop 1
-- 3 machines, 1-3
insert into site_layout (site_id, seq, machine_id, span) 
values
(1,1,1,12),
(1,2,2,12),
(1,3,3,12);


-- Edinburgh factory 2
-- 9 machines  4-12
insert into site_layout (site_id, seq, machine_id, span) 
values
(2,1,12,6),
(2,2,5,6),
(2,3,6,6),
(2,4,7,6),
(2,5,8,6),
(2,6,4,6),
(2,7,11,4),
(2,8,10,4),
(2,9,9,4);

-- Edinburgh SMIC 4
-- 3 machines  13-15
insert into site_layout (site_id, seq, machine_id, span) 
values
(4,1,13,12),
(4,2,14,12),
(4,3,15,12);

-- Minto 7
-- 4 machines  18-21
insert into site_layout (site_id, seq, machine_id, span) 
values
(7,1,20,6),
(7,2,19,6),
(7,3,21,6),
(7,4,18,6);

-- Tomago 6
-- 4 machines   27-30
insert into site_layout (site_id, seq, machine_id, span) 
values
(6,1,27,12),
(6,2,28,12),
(6,3,29,12),
(6,4,30,12);

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();

-- Edinburgh factory
insert into site_layout (site_id, seq, machine_id, span) 
values
(),
(),
();
